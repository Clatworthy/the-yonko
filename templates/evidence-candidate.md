# Evidence candidate summary

Generated by `scripts/evidence-index.py candidate`. Staging only - not canonical.

```markdown
# Evidence candidate

- Evidence id: `<session_id>__<review_type>`
- Activity id: `<ticket or owner-confirmed id>`
- Review type: implementation | plan | document
- Artifact type: pap | prd | adr | design | n/a
- Final status: <approved | rejected | pass | rolled_back | ...>
- Owner: <handle>
- Tickets: <TA-...>
- Concepts: <controlled concepts>
- Candidate hash (record_hash): `<sha256>`
- Artifacts: listed under `evidence-candidate/artifacts/`

## Publish (local only - no commit/push)

scripts/evidence-index.py publish-local \
  --session <session-dir> \
  --candidate-hash <sha256> \
  --approved-by <your-handle>
```

Rules:

- Candidate is opt-in after finalize (or explicit `candidate` CLI).
- Publication requires the candidate SHA-256 and `--approved-by`.
- The CLI never runs `git add`, `git commit`, `git push`, or contacts a remote.
- Canonical means trustworthy and complete, not successful.
